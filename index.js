#!/usr/bin/env node


// The Hash bin line above only goes in the entry file. First line.

// If you are going to just import a file that dosn't import 
// anything, you don't need `from`. This will execute the file below.
import './src/command.js' 
